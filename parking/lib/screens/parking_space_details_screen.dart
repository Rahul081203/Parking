import 'package:flutter/material.dart';

class ParkingSpaceDetailsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Parking Space Details'),
      ),
    );
  }
}
